#include <stdio.h>
void checkPrime(int a)

int main()
{ 
    int result;
    int i;
    int a;
    int count = 0;

    for(i = 1; i < 1000; i++)
    {
        if(a == 1)
	{
            printf("prime\n");
        }
        if(a == 0)
	{
            printf("not prime\n");
    	}
    }
    for(a = 1; a < 1000; a++)
        res = a;
    printf("the result is %d", result);
    
    return 0;
}
