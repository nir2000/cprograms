#include<stdio.h>

int main()
{ 
	/*int x = 10, y = 20;
	if(x == y);
	    printf("%d %d", x, y);
	return 0;

	/*int x = 3, y = 5;
	if(x == 5)
	    printf("%d", x);
	else
	    printf("y value is %d", y);
	return 0;*/

	int i=65;
	char j;
	if(i==j)
	    printf("C is WOW\n");
	else
 	    printf("C is headache\n");
	return 0;
}
